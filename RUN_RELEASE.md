# Direct Run

Run the packaged application from:

```text
build/Release/MedicalImageAnalysisPlatform.exe
```

The `build/Release` directory includes the required Qt runtime DLLs and plugins.

Installer output:

```text
installer/output/MedicalImageAnalysisPlatform_Setup.exe
```

Sample images remain in the `图片/` directory.
