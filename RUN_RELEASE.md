# 直接运行说明

从 GitHub 下载仓库后，可以直接运行：

```text
build/Release/BMPViewerQt.exe
```

这个目录已经包含 Qt 运行所需的 DLL 和插件文件。也可以运行安装包：

```text
installer/output/BMPViewerQt_Setup.exe
```

实验四需要的示例图像已放在 `图片/PET_image.bmp` 和 `图片/factory.bmp`。
